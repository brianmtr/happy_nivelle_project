<div class="container">
<% @events.each do |event| %>

  <td><% if event.status === "accepted" %>
    <div class="cards">
  <div class="card-headers" id="headingOne">  
   
      
        <strong><%= event.title %></strong>
        <%= event.date %>
        <button class="button-agenda">
        <%= link_to 'Voir événement', event_path(event.id) %>
      </button>
         </div>
    </div>
      <% else %>

        <% end %>

    <% end %>

</div>