<h2 class="title-devise">Mot de passe oublié ?</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
<div class="input-button-password-new">
  <div class="field-password-new">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="actions-password-new">
    <%= f.submit "M'envoyer les instructions" %>
  </div>
  </div>
<% end %>
<div class="devise-shared-links-password">
<%= render "devise/shared/links" %>
</div>